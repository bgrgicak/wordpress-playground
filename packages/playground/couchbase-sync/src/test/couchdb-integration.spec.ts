/**
 * @deprecated Replaced by pouchdb-sync-integration.spec.ts which
 * tests the same scenarios without requiring a Docker CouchDB server.
 * PouchDB provides a CouchDB-compatible API that runs in Node.js.
 */
export {};
